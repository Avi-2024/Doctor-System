# Prescription Templates API

Method: `POST | GET | PATCH | DELETE`  
Route: `/api/v1/prescription-templates`, `/api/v1/prescription-templates/:id`  
Description: Manage reusable prescription templates.  
Request: Template payload, UUID path, or pagination query.  
Response: Standard response.  
Permissions: `prescriptions:manage`.
